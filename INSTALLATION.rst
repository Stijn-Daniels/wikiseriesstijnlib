============
Installation
============

At the command line::

    $ pip install wikiseriesstijnlib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wikiseriesstijnlib
    $ pip install wikiseriesstijnlib

Or, if you are using pipenv::

    $ pipenv install wikiseriesstijnlib

